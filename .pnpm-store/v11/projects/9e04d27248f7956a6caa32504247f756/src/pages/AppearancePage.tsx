import { useEffect, useState } from 'react'
import { Check } from 'lucide-react'

export function AppearancePage() { const [theme, setTheme] = useState(() => localStorage.getItem('nexomail-theme') ?? 'light'); useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('nexomail-theme', theme) }, [theme]); return <section className="settings-page"><p className="eyebrow">Configuración</p><h1>Apariencia</h1><p className="page-description">Elige el tema que mejor se adapte a tu espacio de trabajo.</p><div className="theme-options">{['light', 'dark'].map(option => <button key={option} className={`theme-option ${theme === option ? 'selected' : ''}`} onClick={() => setTheme(option)}><div className={`theme-preview ${option}`}><span /><i /><b /></div><strong>{option === 'light' ? 'Tema claro' : 'Tema oscuro'}</strong><small>{option === 'light' ? 'Claro y sobrio' : 'Suave para baja luz'}</small>{theme === option && <Check className="selected-check" size={16} />}</button>)}</div><div className="settings-card palette-card"><h2>Color principal</h2><p>El tema está basado en variables CSS centrales para que puedas cambiar toda la apariencia desde un único archivo.</p><div className="palette"><i /><i /><i /><i /></div></div></section> }
